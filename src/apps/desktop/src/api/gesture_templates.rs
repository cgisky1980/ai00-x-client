use ai00_x_core::service::config::types::{GestureBinding, GestureTemplateConfig, SavedAction};

pub fn builtin_templates() -> Vec<GestureTemplateConfig> {
    Vec::new()
}

pub fn default_bindings() -> Vec<GestureBinding> {
    Vec::new()
}

pub fn default_saved_actions() -> Vec<SavedAction> {
    Vec::new()
}
