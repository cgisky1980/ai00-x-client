/**
 * WallpaperNavScene — scene-specific navigation for wallpaper mode.
 *
 * Wraps WallpaperNav for use inside NavPanel.
 * Registered in nav-registry.ts so NavPanel auto-shows it when wallpaper scene is active.
 */

export { default } from '../../components/WallpaperNav/WallpaperNav';
