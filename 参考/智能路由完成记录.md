# 智能路由机制完成记录

日期：2026-08-21（同日修订：统一三级模型体系；v3：分类器改为 state-embedding + MLP 头）
参考项目：`C:\work\niceui\rwkv-router`（R0-R3 四级分类 + 后处理规则栈 + state-embedding 论文路径）

## 一、功能概述

为 Agent 建立智能路由机制：会话模型选择「自动（auto）」时，由本地 RWKV 模型对用户请求做复杂度分类（R0-R3 四级），按配置将请求路由到对应档位的模型/API；分类失败或引擎未就绪时自动回落。

## v3 修订：logits 方案废弃，改为 state-embedding + MLP 头（实测数据驱动）

v2 的「prompt + 数字 token logits」方案经真实模型（rwkv7-g1i-2.9b.int8）40 样本实测**证伪**：
- v1 裸 prompt：30%（预测塌缩到 R0）；v2 User:/Assistant: 包裹：28%（全部塌缩到 R2）
- 根因：2.9B 基座模型不具备指令遵循能力，logits 里无可信分类信号

v3 改用 rwkv-router 论文路径（`paper/scripts/03_classification.py`，test 93.25%）：
```
用户请求原文
  → tokenize（128 token 截断，空文本补 token 0）
  → 零状态 chunked prefill（forward_seq_mean_hidden）
  → 最后一层 FFN 输出 mean pooling → hidden [2560]
  → 标准化 (h-mean)/std → Linear(2560→256) → GELU(tanh近似) → LayerNorm → Linear(256→4) → softmax
  → 4 类概率 → 后处理规则栈（不变）
```

**端到端实测（2.9B int8，含中文增强训练后）**：
| 评测集 | 准确率 |
|---|---|
| golden held-out 314 条（英文为主） | **97.1%**（R0 100%/R1 95%/R2 96%/R3 100%） |
| 手工 40 条（中英混合） | **100%**（R0-R3 全对） |
| 单次分类延迟 | avg 97ms / p95 106ms |

**训练数据陷阱与修复**：golden_balanced.jsonl 的 1783 条中文样本全部是 R0（闲聊），导致首训模型学到「中文→R0」捷径（中文 R1/R2 全塌缩 R0@1.00，英文任务却 96%）。修复：`gen_zh_augment.py` 生成 857 条中文 R1/R2/R3 模板组合样本（仅并入 train，不污染 held-out），重训后中文手工集 45%→100%，英文 held-out 96.2%→97.1% 无回退。

**附带修复的引擎级 bug（影响所有变长 prefill 场景）**：
1. `clear_cache` 清空 CUDA 全部已编译 kernel（T 变化即触发，每次重编译 ~0.5s）——CUDA kernel 地址是启动参数，与缓冲无关，仅需销毁 prefill graph。修复后单次分类 1056ms→~100ms。
2. **SeqBuffers 显存泄漏**：T 变化重建 seq 缓冲时旧缓冲 TensorId 被 Drop，但后端注册表仍持有设备内存（~50MB/次），190 次变长 prefill 即耗尽 22GB 显存。新增 `free_tensor` 后端 API + `SeqBuffers::free`，在 3 处 T 变化点释放。该 bug 同样存在于生产 chat pool（每条不同长度消息泄漏一次）。

### 统一三级模型体系（local / fast / primary ↔ R0-R3）

不再维护「primary/fast」与「R0-R3」两套概念，模型体系与路由档位直接对应：

| 概念 | 语义 | 对应路由档位 | 配置方式 |
|---|---|---|---|
| **local**（rwkv-local） | 本地 RWKV 模型，零成本轻量任务 | R0 + R1 | 固定，无需配置 |
| **fast** | 中档模型 | R2 | 用户可选，未配置回落 primary |
| **primary** | 旗舰模型 | R3 | 用户可选，全局默认回退 |

内部轻量调用（grep/git/web 工具摘要、上下文压缩、会话标题、Explore/FileFinder/Router 等 agent）语义上属于 R0-R1，全部指向 `rwkv-local`。原「fast 强制绑定 rwkv-local」机制已移除（`enforce_fast_model_binding` 删除，替换为 `migrate_fast_model_refs` 一次性迁移：存量 agent/func-agent 的 "fast" 引用改写为 "rwkv-local"，路由 R0/R1 同步改写，`default_models.fast` 若为强制绑定产物则清空）。

### 分类四级定义（对齐 rwkv-router RouteClass）

| Tier | 含义 | 默认模型 |
|---|---|---|
| R0 | 闲聊（问候/致谢/确认） | rwkv-local |
| R1 | 简单任务（单步操作/简单问答/翻译/查询） | rwkv-local |
| R2 | 复杂任务（代码生成/多步推理/多工具协作） | fast |
| R3 | 高难度（调试诊断/错误分析/长上下文/高风险变更） | primary |

### 处理流水线

```
auto 模式请求
  → ① trivial ack 短路（纯规则，零成本）──命中──→ R0
  → ② RWKV 本地分类：tokenize(128截断) → 零状态 prefill → mean-hidden → MLP 头 → 4 类概率
  → ③ 后处理规则栈：safety 升级（R0/R1 且 P(R2)+P(R3)>0.45 → R2）+ sticky tier（短消息不低于上一轮）
  → tier_models 映射 → model_id → 现有 get_client_resolved 链路
  → 任何失败（引擎/head 未加载、超时、维度不匹配）→ fallback（默认 primary）
```

## 二、关键实现决策

1. **分类器 = mean-pooled 最后一层 hidden + 训练 MLP 头**（rwkv-router 论文路径，本地 2.9B 实测 97.1%/100%）。MLP 头权重 `router_head.json`（14MB）部署在 `models/rwkv/`，engine Init 时加载；缺失/损坏/维度不匹配时分类自动禁用并回落 primary（系统无头文件也正常运行）。
2. **分类走推理池专用零状态**：`PoolRequest::Classify { request }` 在 pool 线程同步执行（128 token prefill ~100ms），独立 `classify_state` 每次从零状态快照恢复，与 16 个生成槽位隔离。
3. **分类引擎复用 rwkv-local 已加载模型**：共享常驻显存，无第二次加载。
4. **自动预加载**：desktop 启动序列 `preload_engine_for_router()` 读 `ai.router.enabled`，为 true 时自动初始化引擎。
5. **仅 auto 模式生效**：用户显式选择具体模型时完全不干预。
6. **sticky tier 会话表**：容量上限 1024 超限清空。

### MLP 头训练管线（可复现）

```
1. 数据：client/scripts/router_head/data/golden_balanced.jsonl（16,751 条，源自 rwkv-router）
   + zh_augment.jsonl（857 条中文增强，gen_zh_augment.py 生成）
2. 特征提取：rwkv-rsv/examples/extract_router_features.rs
   （DATA/OUT 环境变量指定输入输出，断点续跑，~10 样本/秒）
3. 训练：cd client/scripts/router_head && uv run train_mlp.py
   --data data/features.jsonl data/zh_features.jsonl
   （AdamW 1e-3 + Cosine 30ep + bs 256 + clip 1.0，dev early stop；
    70/15/15 分层切分，增强集仅入 train；输出 dev/test 准确率 + router_head.json）
4. 部署：copy router_head.json → models/rwkv/router_head.json，重启应用生效
```

## 三、代码位置

### 新增
| 文件 | 职责 |
|---|---|
| `src/crates/core/src/agent/routing/head.rs` | RouterHead：MLP 头加载（JSON 契约）+ forward（标准化→Linear→GELU→LN→Linear→softmax），含单测 |
| `src/crates/core/src/agent/routing/mod.rs` | SmartRouter（路由流水线、sticky 表、全局单例） |
| `src/crates/core/src/agent/routing/tier.rs` | RouteClass R0-R3（移植 rwkv-router tier.rs） |
| `src/crates/core/src/agent/routing/rules.rs` | trivial ack 短路 + 短消息判定（中英闲聊词表） |
| `src/crates/core/src/agent/routing/postprocess.rs` | 后处理规则栈 + RoutingDecision |
| `rwkv-rsv/src/gpu_model.rs` 新方法 | `forward_seq_mean_hidden`（prefill 后 T 维 mean pooling，跳过 head） |
| `rwkv-rsv/examples/extract_router_features.rs` | 训练特征提取工具（断点续跑） |
| `client/scripts/router_head/` | uv 训练项目（train_mlp.py + gen_zh_augment.py + pyproject.toml） |
| `src/web-ui/src/infrastructure/config/components/SmartRouterConfig.tsx` | 设置页智能路由 UI |

### 修改
| 文件 | 变更 |
|---|---|
| `core/src/service/config/types.rs` | AIConfig 新增 `router: RouterConfig` |
| `core/src/agent/execution/execution_engine.rs` | `resolve_model_id_for_turn` auto 分支接入 SmartRouter |
| `ai-adapters/src/providers/rwkv/engine.rs` | trait 方法 `classify(request) -> Vec<f32>`（4 类概率，默认 Err） |
| `apps/desktop/src/rwkv_llm.rs` | Classify 请求（tokenize→mean-hidden→RouterHead）+ head 文件加载 + rwkv_classify + init_engine_internal + preload_engine_for_router |
| `apps/desktop/src/rwkv_engine_adapter.rs` | 实现 classify（桥到 rwkv_classify） |
| `apps/desktop/src/lib.rs` | 启动序列挂载 preload_engine_for_router |
| `apps/desktop/src/api/config_api.rs` | invalidate 列表加 `ai.router` |
| `web-ui .../AIModelConfig.tsx` | 默认模型 section 后挂载 SmartRouterConfig |
| `web-ui .../types/index.ts` | SmartRouterConfig/RouterTierModels 类型 |
| `locales/{zh-CN,en-US}/settings/default-model.json` | smartRouter 文案段 |

## 四、配置说明（app.json `ai.router`）

```json
{
  "ai": {
    "router": {
      "enabled": true,
      "tier_models": { "r0": "rwkv-local", "r1": "rwkv-local", "r2": "fast", "r3": "primary" },
      "fallback": "primary",
      "safety_threshold": 0.45,
      "sticky_enabled": true,
      "timeout_ms": 3000
    }
  }
}
```

tier 值可为已配置模型 id 或逻辑名 "primary"/"fast"/"rwkv-local"。前端入口：设置 → AI 模型 → 智能路由。各 tier 下拉固定提供三个逻辑选项：主力模型、中档模型、RWKV 本地模型（直接走本地推理，无需网络），外加用户已启用的模型列表；rwkv-local 未在用户模型列表中时由后端内置默认配置兜底。

默认模型页（DefaultModelConfig）：primary 下拉 + fast 下拉（现为自由中档槽位，不再绑定 RWKV；未配置回落 primary）。模板页/功能 Agent 选择器（ModelSelectionRadio）新增「RWKV 本地模型」单选项。

## 五、附带完成：memory sidecar 与记忆系统整体删除

本次任务同时清理了已取消的 memory sidecar 及整个记忆系统（sidecar 是其自动提取与相关性验证核心，取消后价值过低）：
- 删除 `core/src/service/memory_graph/`（10 文件）、`memory_tool.rs`、`desktop/src/memory_sidecar.rs`、前端 MemoryHint 组件
- 清理 coordinator（MemoryAgent 启动/4 处上下文发送）、prompt_builder（AgentMemoryInject）、execution_engine（MemoryInjected 事件）、events/transport 事件映射、model_init 初始化调用、`rwkv_infer_sync`
- 孤儿数据文件 `memory_graph.json`（用户数据目录）不影响运行，可手动删除

## 六、验证结果

- **端到端准确性（真实模型 2.9B int8 + 训练头）**：golden held-out 314 条 97.1%；手工 40 条（中英混合）100%；单次延迟 avg 97ms / p95 106ms
- **训练指标**：dev 96.9% / test 96.0%（golden held-out）；Python 导出与 Rust 实现数值零误差（max diff 0.000000）
- `cargo test --release -p ai00-x-core routing`：24 个单元测试全绿（tier 往返/trivial ack/safety/sticky/RouterHead 数学）
- `cargo clippy --release --all-targets`（core/adapters/desktop + rwkv-rsv lib/examples）：零警告
- `cargo check --release -p ai00-x-desktop`：通过
- `pnpm type-check:web` + `pnpm lint:web`：通过

## 七、已知限制与后续方向

- **head 与模型绑定**：更换 rwkv 模型（n_embd 变化）后需重跑训练管线（head 自动禁用并回落 primary，无报错）
- **训练数据语言覆盖**：英文 86% / 中文 11%（增强后覆盖 R1-R3 任务型中文）；其他小语种未验证
- **分类延迟 ~100ms**：128 token 全量 prefill；后续可做分桶等长复用（论文路径 albatross 的 bucketing）进一步压缩
- **范围外**（rwkv-router 有、本版未做）：SQLite 在线样本捕获、自学习重训闭环、head 热更新（当前需重启）

## 八、生产环境集成（2026-08-21）

### 分发链路

`router_head.json`（14.2MB，含中文增强重训版）已纳入模型分发体系，与 vocab/权重同链路：

- **注册**：`sync-models.py` 自动扫描 `.ai00-x-dev/models/rwkv/`，manifest `components.rwkv.models` 新增 `router_head` 条目（`required: true`，sha256: `69e830...b180`）
- **上传**：ModelScope（cgisky/Ai00-X）与 HuggingFace（cgisky/ai00-x）双端已推送，远程 manifest 均已确认注册成功
- **下载**：新用户首启 → `model_checker.check_updates` 检测本地缺失 → 随 rwkv 组件自动下载（仅 +14.2MB）；老用户下次模型检查时增量下载
- **加载**：引擎初始化时从 `{models_dir}/rwkv/router_head.json` 读取（`rwkv_llm.rs` 的 `assets_models_dir()`）；缺失/损坏/维度不匹配均自动降级回落，不阻塞启动

### 训练产物位置

- 训练脚本：`client/scripts/router_head/`（train_mlp.py、gen_zh_augment.py）——**尚未入 git**，提交时注意排除 14MB 的 `router_head.json` 产物（已通过模型仓库分发，无需入库）
- 训练产物（权威源）：`client/scripts/router_head/router_head.json` = `client/.ai00-x-dev/models/rwkv/router_head.json`（字节一致）

### 与灵感来源论文对比（SquillaRouter, arXiv:2607.11399）

论文《Agentic Routing: The Harness-Native Data Flywheel》（TokenRhythm/OpenSquilla，2026-07）：

| 维度 | SquillaRouter（论文） | ai00-x |
|------|----------------------|--------|
| 特征 | 390 维手工拼接（长度/语言/代码/关键词+外部 embedding） | 2560 维 state-embedding（复用 RWKV 引擎，零额外依赖） |
| 分类器 | LightGBM + ONNX | 端到端 MLP，全 Rust |
| 分级 | C0–C3 | R0–R3（概念继承） |
| 论文核心结果 | PinchBench 成本 -90%、DRACO -43%（质量保持） | 未做成本基准（R0/R1→本地即成本为零） |
| 分类准确率 | 未直接报告 | test 96.0% / 端到端 97.1% |
| 自学习飞轮 | 有（在线累积+定期重训） | 未实现（最大差距，后续方向） |

对比本地方法论文（C:\work\niceui\rwkv-router\paper\paper.md，0.4B 骨干）：test_acc 93.25% → 96.0%（+2.75pp，骨干 0.4B→2.9B + 中文增强）。

## 九、生产化改进轮次（2026-08-21，计划文档 .trae/documents/smart-router-improvements-plan.md）

按五阶段计划完成（自学习闭环经用户决策明确排除：单用户数据偏向性导致过拟合，需专门去偏方案后另行设计）：

### Phase A：P0 逻辑修复
1. **"继续"移出 trivial-ack 词表**（rules.rs）：它是任务推进指令而非确认词，R3 任务中段"继续"被短路到 R0 会导致本地小模型接手复杂任务
2. **trivial-ack 不再写入 sticky 表**（mod.rs）：R3 任务中"ok"→"继续"场景，sticky 参照保持 R3，短消息被正确提回
3. **分类失败不写 sticky 表**：瞬态失败（引擎忙/超时）不再拉低会话档位下限

### Phase B：分类器健康状态 + 测试入口
- `RouterHeadStatus` 全局状态（rwkv_llm.rs，RwLock）：loaded/input_dim/detail（缺失/解析失败/维度不匹配/引擎未初始化）；head 加载提取为 `load_router_head(num_emb)`，加载时即校验维度（原先运行时才发现）
- `route_preview()`（core）：无状态路由预览，不读写 sticky 表
- Tauri 命令：`get_router_status` / `test_router_classification` / `reload_router_head`（config_api.rs + lib.rs 注册）
- 设置页（SmartRouterConfig.tsx + RouterTestPanel.tsx）：三态状态行（绿已加载/黄未加载+原因/灰引擎初始化中，5s 轮询至就绪）+ 测试框（输入任意文本 → R0-R3 概率条 + 路由结论）

### Phase C：路由决策前端可视化
- `ModelRoutingInfo`（events crate）：tier/source/confidence/model_ref/safety/sticky，挂入 `ModelRoundCompleted` 事件（可选字段，向后兼容）
- 管道：`resolve_model_id_for_turn` 返回 `(model_id, Option<ModelRoutingInfo>)` → RoundContext → round_executor → tauri.rs 桥（modelRouting 键）→ EventHandlerModule → ModelRound.routing
- 聊天界面：`RoutingBadge` 组件（ModelRoundItem 首轮顶部渲染 `Auto · R2 · fast · 87%`，悬浮显示完整决策含 safety/sticky 标记）

### Phase D：配置 UX
- tier/fallback 引用已禁用模型时：下拉保留该选项并加"（已禁用）"后缀；引用不存在的模型时显示黄色警告行
- 高级参数折叠区：safety_threshold（0.1-0.9）/ sticky_enabled / timeout_ms（500-10000）

### Phase E：引擎健壮性
- sticky 表 LRU：HashMap + VecDeque 插入序，超 1024 逐个淘汰最旧会话（原为全清）；更新时刷新新鲜度
- head 热重载：`PoolRequest::ReloadRouterHead` pool 消息 + `rwkv_reload_router_head()` + 设置页"重新加载"按钮——router_head.json 更新后无需重启应用

### 验证
- `cargo fmt --all --check` 通过；`cargo clippy --release`（core/transport/desktop）零警告
- `cargo test --release -p ai00-x-core routing`：29 个测试全绿（新增 6 个：继续非 trivial、sticky 三重防污染、LRU 淘汰/刷新）
- `pnpm type-check:web` + `pnpm lint:web`：零错误

## 十、上下文感知路由：会话摘要方案（2026-08-21，计划 .trae/documents/router-context-summary-plan.md）

**用户定案**：方案 B 模型摘要（每会话维护滚动摘要、控制字数）；取消 128 token 限制（256 = 摘要 128 + 请求 128，恰好 GEMM m_pad 对齐档，延迟不变）；**训练分布必须镜像线上——有摘要是大概率事件（第二轮起必有），故主数据集 71% 为带摘要格式**。

### 运行时管线
- **SessionSummaryService**（`routing/summary.rs` 新增）：LRU 缓存（512 会话）+ in-flight 去重；每轮对话完成后异步 spawn 更新（continuation 风格 prompt 适配无指令遵循的基座模型；低温 0.3、150 token、~2.7s）；后处理剥回声/双换行截断/词数预算 120；失败保留旧摘要（首条失败→无摘要，均为已训练分布）
- **分类输入格式**：`Summary: {摘要}\nRequest: {请求}`（有摘要）/ 裸请求（无摘要）；`build_classify_input` 保证训练/线上严格一致
- **CLASSIFY_MAX_TOKENS 128→256**：双段预算；超长输入防御（防秒级 prefill/超时/OOM），复杂度判断无需全文
- route/route_preview 增加 `summary` 参数；设置页测试入口传 None（裸分类）

### 训练数据集重设计（关键转折）
首版仅附加 1,262 条合成摘要样本（占 5%），上下文评估 43.3%→56.7% 但 R2 塌缩（3/11）——分布颠倒。重设计：
- **build_dataset.py（新增）**：golden 16,751 条真实请求为主体，**71% 配语义同档摘要**（tier 不变；R0/R1→轻聊摘要池、R2→中档任务池、R3→重调试池，各池 12-16 条真实风格摘要），29% 保留裸请求；每条请求只出现一次（无跨格式泄漏）
- **gen_context_augment.py**：边界配对样本（"继续"+调试摘要→R3 / +闲聊摘要→R0；"改成中文"+翻译摘要→R2），三档显式标注（首版 mid 档推导规则与评估标注不一致是 R2 塌缩主因），1,457 条仅入 train
- **extract_router_features.rs**：MAX_TOKENS 默认 256（环境变量可覆盖），与线上一致

### 重训结果（router_head_context.json，13.5MB）
| 指标 | 旧 head | 新 head（v2 数据集） |
|---|---|---|
| golden held-out dev/test | 96.9% / 96.0% | **98.3% / 98.3%**（+2pp：摘要本身即强复杂度信号） |
| 上下文边界评估（30 条手工集） | 43.3%（接近随机） | **86.7%**（R2 3/11→10/11；剩余 4 失误均相邻档，线上 safety/sticky 兜底） |

### 部署
- 本地三处（脚本权威源 + .ai00-x-dev/models + target/release/models）+ ModelScope/HF 双端 manifest 已推送；老用户下次模型检查增量更新，或设置页"重新加载"按钮热重载

### 验证
- `cargo fmt --check` 通过；clippy（core/desktop）零警告；routing 34 测试全绿（新增 summary 5 个：prompt 构造/截断/清洗/LRU/格式）

## 十一、摘要池扩充：真实多轮对话数据集（2026-08-21）

**动机**：v3 摘要池仅 232 条且来源单一（手构场景——assistant 回复是占位符文本，非真实对话；MT-Bench 问题 + 82 条真实风格场景），R0:31 / R1:48 / R2:132 / R3:21 分布失衡，分类器在轻/重档边界判断不准。

### 数据源（hf-mirror 下载，`data/multiturn/raw/`）
| 数据集 | 内容 | 场景覆盖 |
|---|---|---|
| ShareGPT-Chinese-English-90k（shareAI）4 文件 | 真实用户-助手多轮对话（`common_zh/en_70k` + `computer_zh/en_26k`） | 中英 ×（日常闲聊/问答/写作/编程/调试），assistant 回复真实 |
| lmsys/mt_bench_human_judgments parquet | MT-Bench 80 题配 GPT-4/3.5 真实回答 | 写作/角色扮演/推理/数学/编程/抽取/STEM/人文 8 类 |

### 管线（可复现）
1. **sample_scenarios_v2.py**：从原始集采样 178 个 2-4 轮对话场景（`scenarios_v2.jsonl`），启发式 tier 标注——computer 文件：debug 关键词命中≥2（或命中≥1 且 ≥3 轮）→R3 否则 R2；common 文件：代码块→R2、短消息+闲聊词→R0、其余→R1；MT-Bench 按类目映射（roleplay→R0 / writing·stem·humanities→R1 / math·coding·extraction→R2 / reasoning→R3）。单消息截断 2000 字符控 prefill 成本
2. **batch_summarize.rs**：178 场景逐轮滚动生成 467 条真实摘要（与线上同管线，0 拒绝）
3. **merge_summaries.py**：质量过滤（<30 字符或 <6 词无信号；乱码符号密度>2% 如 tokenizer 退化 "✿end33✿"）+ 去重，旧 232 + 新 467 → **536 条池**（R0:107 / R1:135 / R2:213 / R3:81，丢弃 163 条低质量）

### 重训结果（router_head_v3.json，13.5MB）
| 指标 | v3（232 池） | v3.1（536 池） |
|---|---|---|
| golden held-out test | 97.1% | **97.7%**（无回退） |
| 真实摘要边界评估集（52 条） | 67.3% | **69.2%**（36/52；miss 全为相邻档混淆，R0 7/12 R1 11/14 R2 15/21 R3 3/5） |

边界集为设计内困难样本（同一"继续"类模糊请求配不同档摘要），相邻档混淆受 safety/sticky 后处理兜底；R3 仅 5 条样本统计意义有限。

### 训练配置
dataset_v3（16,751 条，70% 带真实摘要）+ context_augment_v3（3,063 边界样本，仅入 train）；AdamW 1e-3 + Cosine 30ep，dev best 97.9%。

### 部署与验证
- 三处部署（脚本权威源 + .ai00-x-dev/models + target/release/models）
- `cargo fmt` + clippy（rwkv-rsv examples / ai00-x-core）零警告——顺带等价重构三处（summary.rs / batch_summarize / summarize_probe）words 计数 `if/else if` 合并为 `||`（语义逐字一致冻结约定保持）
- routing 35 测试全绿

### 后续方向
- 摘要质量是当前瓶颈：2.9B 摘要在长真实对话上偶发退化/无信息输出（过滤丢弃率 25%），可考虑加大池量对冲或摘要提示词微调
- 边界评估集仅 52 条，可扩至 ~200 条降低统计噪声

## 十二、v4：PrevTier 数值特征 + 真实对话逐轮切片（2026-08-22，计划 .trae/documents/router-prevtier-multiturn-slices-plan.md）

**用户指出 v3 两个根本缺陷**：① 训练数据是"随机摘要×随机请求"拼凑——语义割裂，正确做法是一个多轮对话逐轮切片（第 k 轮样本 = 前 k-1 轮滚动摘要 + 第 k 轮真实输入 + 上一轮路由等级）；② 上一轮等级（PrevTier）应作为**数值特征进 MLP**而非提示词文本（2.9B 基座对文本元指令不可靠，logits 方案已证伪）。

### 契约改造（向后兼容）
- **head JSON 新增可选 `base_dim`**：存在时 `input_dim = base_dim + 5`，输入 = hidden[base_dim] ‖ prev_tier_onehot[5]（R0-R3 + None 位）；无 base_dim 的旧 head 在新代码下原样运行（prev_tier 忽略）。`forward(hidden, prev_tier)`；引擎维度校验改用 `expected_hidden_dim()`
- **传参链**：`engine.classify(request, prev_tier)`（trait/adapter/PoolRequest/rwkv_classify）；SmartRouter.route() 在 classify **前**读 sticky 表（此前只在后处理读）——同一 prev_tier 供分类与 sticky 后处理
- **extract_router_features.rs**：特征行透传 `prev_tier`（None 不序列化）；train_mlp/eval_head 统一拼 one-hot（index 4 = None）
- 单测 38 个全绿（新增：v4 契约解析/维度拒绝/onehot 改变输出/旧 head 兼容/非法 prev_tier→None）

### 训练数据（真实切片，一个对话多次利用）
- **v2c 批次**：ShareGPT 再采样 1,596 场景（scale 14、轮数上限 6、与 v2/v2b 去重）→ 5,216 条滚动摘要（0 拒绝）
- **build_dataset_v4.py**：四批场景+摘要按 (scenario, turn) 对齐逐轮切片——turn 0 = 裸请求（prev_tier=None）；turn k≥1 = `Summary: {真实滚动摘要}\nRequest: {真实第 k 轮输入}` + prev_tier=上一轮标签；摘要被过滤 → 降级裸请求+prev_tier（线上失败分布）。逐轮打标：确认类继承上一轮（**上下文教学核心**）、故障延续→R3、computer+debug→R3、代码→R2、短闲聊→R0、其余→场景背景。**场景级 85/15 hash 切分**（对话不跨 train/eval）
- 产出：train 5,603 切片（全上下文 2,370 / 带 prev_tier 裸 1,444 / turn-0 1,789；tier 1614/1409/1418/1162）、eval 1,045；boundary 压力集 208 条（摘要仅取 eval 侧场景，零泄漏）
- train_mlp 新增 `--context-ratio`（上下文样本过采样；**实测无过采样最优**，扫描 r50/r30/r0/ctx-only）

### 结果（router_head_v4.json，部署三处 + ModelScope/HF 已同步）
| 指标 | v3（合成配对） | v4（真实切片+PrevTier） |
|---|---|---|
| golden held-out test（裸请求） | 97.7% | 94.6%（-3pp：切片噪声标签的代价，sticky/safety 后处理兜底） |
| **真实切片评估 slices_eval（1,045 条）** | **32.5%** | **79.3%**（+47pp） |
| 其中上下文样本（摘要+prev_tier） | 32.9% | **94.4%** |
| 其中裸样本（turn-0/摘要失败） | 31.8% | 47.2% |
| boundary 压力集（208 条） | 46.6% | 46.2%（持平；该集标注本身有主观性，真实切片上的边界行为已由 ctx 94.4% 证明） |

### 附带：GPU 后端大并发改造（rwkv-rsv，batch_summarize 提速 1.8×）
用户要求"单线程多并发、不加载多个模型"：
- **异步 pinned 上传**：selfloop 逐轮 sampler 参数走 pinned 暂存 + `cuMemcpyHtoDAsync`（流序）——消除逐轮 `cuStreamSynchronize` 的 CPU⟷GPU 往返（旧 0.41/s → 0.73/s，**1.8×**）
- **拆分 submit/collect**：selfloop 全异步提交即返回，收集时各流独立同步
- **权重共享多流**：`import_tensors_from`（同一 primary ctx 设备指针跨实例有效，导入张量不拥有）+ `build_shared`——3GB 权重只载一份，K 实例各占百 MB 级私有缓冲；kernel 缓存共享（省每实例 ~30s 编译）
- **K 扫描定论**（120 场景同数据）：K=1: 0.75/s / K=2: 0.76/s / K=4: 0.74/s——**极限平衡点 K=1~2（等价）**，GEMV decode 单流已打满该卡可达带宽；生产跑 K=2
- **stream 改 CU_STREAM_NON_BLOCKING**：与 legacy NULL stream 解耦（阻塞式 stream 下同步拷贝会排干全部流）；常规上传改 pinned scratch + 流序异步（pageable 源异步拷贝走驱动共享 staging，多线程互踩；同步拷贝则与非阻塞流无序——kernel 先于 DMA 启动读脏数据）
- **并行测试暴露并修复潜伏 OOB**：`norm_matches_cpu` 误传 `rows=batch*h`（grid=rows*h → b 越界读写，单跑侥幸通过、并行时越界写踩其它测试张量）→ 修为 `rows=batch`；全量测试 5 连跑全绿（51/51）

### 已知代价与后续
- golden 裸请求 -3pp：切片启发式标签噪声；后续可尝试样本加权或切片标签人工抽检修正
- boundary 合成集持平：其标注映射偏主观；真实边界行为以 slices_eval 的 ctx 94.4% 为准
- 摘要过滤丢弃率仍 ~25%（质量瓶颈不变）

## 十三、v5：数据规模扩样 4× + segmean 批量特征提取（2026-08-22）

**动因**：v4 训练数据量不足（train 5,603 切片），特征提取串行太慢；用新并发管线重做全量训练/测试数据。

### 数据扩样与摘要重生成
- **sample_scenarios_v5.py**：ShareGPT 四文件配额采样 8,000 新场景（四 tier 各 ≥1,500，与旧 4 批全部去重，--max-turns 6）→ 合并批 `scenarios_all_v5.jsonl` 共 **10,123 场景 / 32,620 轮次**
- **摘要全量重生成**：batch_summarize BATCH=16 单进程单模型并发（信天翁 B×T rows prefill + batch selfloop decode），1.09/s、32,620 条全部完成 **0 拒绝**；中途一次偶发 0xC0000005 崩溃（非超长输入——user ≤2,000 chars 已限）→ 外层 PowerShell 自动重启循环（尾行修复 + resume 按 (scenario,turn) 断点续跑）恢复，后续 7.5h 无再崩
- **build_dataset_v5.py**：新 split seed 85/15 场景级切分 + summary_ok 回声过滤强化（拒绝 `(none)`/`Response to`/`User:` 开头与正文回声）→ **train 27,551 / eval 5,059 / eval_new 4,049**（eval_new = 纯 v5 新场景，v4 头零重叠的干净对比集）；ctx 样本占 69%（真实多数）；tier 分布 7697/6621/6561/6672
- **build_eval_v5.py**：boundary 压力集 26→40 边界（320 条，摘要仅取 eval 侧池，零泄漏）

### GPU 批量特征提取（segmean kernel，1.07/s → 17.4/s，16×）
- **backend_cuda.rs 新增 segmean kernel**：x [batch, T_pad, C] 按 lens[b] 分段均值 → out [batch, C]（pad 行不计入），语义对齐单序列 forward_seq_mean_hidden；`segmean_matches_cpu` 单测 GPU vs CPU 对齐（max_diff < 1e-4）
- **gpu_model.rs `forward_seq_batch_mean_hidden`**：B 个可变长 prompt 一次 batch prefill（GEMM M 维 = B×T_pad，权重读一份算全部行）+ segmean 收割
- **extract_router_features.rs 批量模式**：`BATCH=16` 环境变量启用；断点续跑保留；train 27,551 条 26 分钟提完
- **批量 vs 单序列一致性验证**（部署正确性关键——线上 classify 走单序列路径）：同 8 样本两种模式提取 hidden，**max_abs_diff = 0.000000（位级一致）**，训练/线上分布零错位

### 结果（router_head_v5.json 默认档，部署三处 + ModelScope/HF 已同步）
| 指标 | v4（原部署头） | **v5 默认档** | v5 r50 |
|---|---|---|---|
| golden held-out test（同 split） | 94.6% | 93.9%（噪声内） | 94.0% |
| **eval_new 干净集（4,049 条）** | 77.3% | **80.4%（+3.1pp）** | 80.0% |
| — 其中上下文样本 | 93.0% | 93.9% | **94.4%** |
| — 其中裸样本 | 41.9% | **50.0%** | 47.6% |
| boundary_v5（320 条） | 41.9% | **42.2%** | 41.9% |
| boundary_v4（208 条） | 46.2% | 46.2% | **48.6%** |

选 **v5 默认档**部署：eval_new 总分 / 裸样本 / boundary_v5 三项最高，且与 v4 部署档同为无过采样配方（训练分布 = 数据自然分布）。

### 训练配方（三档 context-ratio 对比）
- 数据：golden 16,751 + zh 增强 857 + v5 切片 27,551 = **40,135**（v4 的 7.2×）
- 默认（无过采样，ctx 69%）/ r50 / r30 三档：r30 无效果——数据自然 ctx 占比 69% > 0.3，oversample 只能上调无法降采样；r50 与默认差异在噪声内

### 遗留与后续
- **裸样本 ~50% 归因分析（2026-08-23，数据驱动结论）**：裸样本全部是 turn-0 首轮切片，其中 **92.6%（1,151/1,243）的标签来自「场景背景兜底」**（label_turn 规则 1/6：确认类与无特征文本继承场景 tier），而场景 tier 是采样时按 ShareGPT 来源域**配额强加**的（common→R0/R1、computer→R2/R3），与文本自身复杂度只是弱相关——标签与特征（纯文本）系统性错位。量化拆分：**文本能证规则组（debug/代码/闲聊命中，n=92）80.4% vs 背景兜底组（n=1,151）47.4%**；混淆矩阵显示错误集中在 R1（32%）/R2（31%）——恰是 common/computer 域配额槽位重叠区，模型按文本真实复杂度散布预测（如「预订机票的分配和团体预订区别」标 R2、模型预测 R0——模型对、标签错；golden 人工标注裸请求 93.9% 排除模型能力问题）。**结论：这是评测基准标签缺陷而非模型缺陷**；改进方向：turn-0 标签改按文本自身复杂度标注（模型+规则或人工抽检）、或 turn-0 不进切片训练集（裸请求由 golden 人工标注数据专责）、或场景采样按内容启发式定 tier 而非随机配额
- **batch_summarize 偶发 0xC0000005**（约 8h 一次，原因未定位）：外层自动重启循环为当前缓解方案；后续可加 CUDA error 回查定位
- r30 档需降采样实现（当前 train_mlp oversample 单向）

## 十四、v5.3：10,120 场景全量人工判级重打标 + golden 融合（2026-08-24）

**动因**：十三章遗留的「标签缺陷」根治。中间方案 v5.1（仅修 turn-0）/ v5.2（规则优先 + golden 裸文本兜底全链，`relabel_turn0.py`）已训练但**未部署**——兜底仍是模型预测，自洽捷径只是被稀释（后验：v5.2 头在人工标签 eval 仅 32.1%，其 golden 98.7% 属循环分数）。

### 人工标注战役（manual_labels.db）
- **label_db.py**：SQLite WAL + `PRIMARY KEY(gid,turn)` + `INSERT OR IGNORE`（幂等防重写），多 sub-agent 并发断点续跑；`dump` 单场景视图带 golden/继承/域三级建议标签（仅参考，agent 独立判级）
- **进度**：9,921/10,120 场景全量标注完成（98.0%），**31,879 轮**；未标 199 个整场景剔除不进训练：~60 个风险内容主动跳过 + 2 个断点块（4870-4963、5078-5149，agent 反复中断区）+ 散点
- 判级标准（campaign rubric）：0=闲聊/问候/讲故事/起名/诗/角色扮演；1=知识问答/写作改写/概念解释；2=代码/SQL/命令行/前后端/排障/系统设计；3=极难（复杂数学证明/点名最强模型）

### v5.3 重打标（relabel_manual.py）
- 人工标签直接替代 v5.2 全部标签来源；文本构造（turn-0 裸文本 / Summary+Request 拼接）、split（seed v5, 85/15）、确认/指代继承 ×8 boost 均与 v5.2 逐项一致（唯一变量 = 标签来源，干净 A/B）；hidden 全量复用 v5 特征（零重提取）
- 数据：train 26,921 + 继承 boost 4,326 / eval 4,958 / eval_new 3,948；人工标签 vs v5.2 标签 eval 侧变化 **77.4%**（兜底链噪声规模的直接度量）
- tier 分布（train 含 boost）：R0 6,355 / R1 13,104 / R2 11,118 / R3 670——campaign 标准下 R3 真实占比仅 ~2%

### 结果（关键：以人工标签为 ground truth 的独立评估）
| 指标 | v5（部署档） | v5.2 | v5.3 纯人工 | **v5.3g（部署）** |
|---|---|---|---|---|
| 人工标签 eval（4,958） | — | 32.1% | 76.2% | **76.8%** |
| 人工标签 eval_new（3,948） | — | 34.5% | 78.9% | **79.3%** |
| golden 裸文本（16,751） | 93.9%* | 98.7%* | 64.0% | **97.1%** |
| — golden R3 召回 | — | — | 2.7% | **95.3%** |
| boundary 人工集（320） | 42.2% | 30.0% | 39.1% | **39.7%** |
| 人工 eval 过度/不足配比 | — | 14.8%/53.2% | — | **10.4%/12.7%** |
| 人工 eval 平均预测档位（真实 1.20） | — | 0.67 | 1.17 | 1.17 |

\* 循环分数：v5/v5.2 的兜底标签源自 golden 头预测。

- **v5.3 纯人工暴露两套标准分歧**：① R0/R1 线偏移（campaign 把简单事实问答/冷知识归 R1，golden 归 R0，1,952+571 行，方向偏保守无害）；② **R3 塌缩**（campaign R3 门槛极严 → 模型几乎不预测 R3，golden 1,751 个难样本 92% 被降档）——golden R3 是合成强样本（复杂分析类），真实流量中确实稀有，但线上存在 R3 模型档位，不可丢
- **v5.3g = v5.3 人工切片主训练 + golden 16,751 作 train-only 增强**（`train_mlp.py --data slices_v53_train_features.jsonl data/features.jsonl`）：两套标准在特征空间兼容，融合后人工标准几乎无损（+0.6pp）、golden R3 召回 2.7%→95.3%、boundary 同步最高；混淆矩阵预测分布 {931,2336,1588,103} ≈ 真实 {960,2166,1722,110}（v5.2 把 67% 流量倒进 R0）
- 诊断工具：`test/diag_head.py`（任意 head×features 混淆矩阵/过度不足配比/预测分布/bare-ctx 拆分）、`test/confusion_golden.py`

### 部署（v5.3g → router_head.json）
- 三处拷贝 MD5 一致（.ai00-x-dev / target\release / target\debug），manifest sha256 `8e9d92c2…4e3161`（14,180,882 B），ModelScope + HuggingFace 已推送
- 契约同构（base_dim 2560 + 5 维 one-hot，同一 Rust forward），热重载 `reload_router_head()` 直接生效

### 遗留与后续
- boundary 人工集 39.7% 仍低：近边界四分类本质难 + 少量 campaign 标注噪声（如「600 字博客」标 R0）；boundary 集自身标签也带主观映射，绝对值仅供参考
- 人工标准下 R3 流量占比 ~2%、eval 召回 41%：R3 触发语义（failure 延续规则标 R3 的样本模型学不稳）待线上 sticky 行为对冲观察
- r30 降采样仍未实现；摘要过滤丢弃率 ~25% 不变

### 训练配方扫参（2026-08-24，五变体，结论：维持 v5.3g 不变）
train_mlp.py 新增 `--class-weights` / `--label-smoothing` 开关后扫参（均基于 v5.3g 配方单变量改动）：
- **R3 权重 ×3/×6**：人工 eval R3 召回 41%→46/45%，但 R1/R2 召回 -3~5pp（承载 78% 流量），总分 75.1% 双双劣化；golden 99.7%+ 系加权后对训练内数据的过拟合，非泛化——R3 人工样本在特征空间与 R2 本征重叠（多为 failure 延续语义），提权换不来可分性
- **context-ratio 0.8**：全线小幅劣化（eval 75.7%），自然 ctx 占比 69% 已近最优
- **label smoothing 0.1/0.05**：唯一真实交换——自然分布 eval/eval_new +0.5~1.0pp（77.8%/80.4%），但 boundary 压力集 -3.8pp（12/320）。判定为噪声边缘交换，路由错误的用户代价集中在近边界处，**保持 v5.3g 部署**
- 可调杠杆已榨尽；进一步提升需数据侧（R3 专项标注、boundary 标签清洗）或特征侧（更强 base 模型）动作


